import Config

# Illume ships as an escript run locally (`./illume ...`) or via
# `mix illume.server` (both dev-env by default) — there is no `mix release`
# / production deployment path in this project's scope, so this file has
# nothing to configure. Present only because `config/config.exs` always
# imports a file per `config_env()`.
